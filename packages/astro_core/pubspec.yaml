name: astro_core
description: Astro poz simulatoru — saf Dart hesap cekirdegi. Flutter bagimliligi YOK.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

# Paket saf Dart kalir — asagidaki bolum bir BAGIMLILIK degil, sadece
# Flutter araclarina "bu paketin asset'i var" demenin yolu. astro_core
# hicbir Flutter API'si import etmez.
flutter:
  assets:
    - assets/stars_bsc5.bin
